<!DOCTYPE html>
<html lang="en">

<head>
     <meta charset="UTF-8">
     <title>To-Do Dashboard</title>
     <link rel="stylesheet" href="/style.css">
</head>

<body>
     <nav class="navbar">
          <div class="brand">CR To-Do</div>
     </nav>
     <div class="container">
          <div class="header-action">
               <h2>Dashboard</h2>
               <a href="/add-task" class="add-btn">+ Add Task</a>
          </div>

          <table>
               <thead>
                    <tr>
                         <th>Date</th>
                         <th>Task</th>
                         <th>Description</th>
                         <th>Status</th>
                         <th>Actions</th>
                    </tr>
               </thead>
               <tbody>
                    <% result.forEach(function(task){ %>
                         <tr>
                              <td>
                                   <%= task.taskDate %>
                              </td>
                              <td>
                                   <%= task.taskTitle %>
                              </td>
                              <td>
                                   <%= task.taskDescription %>
                              </td>
                              <td>
                                   <%= task.taskStatus %>
                              </td>
                              <td>
                                   <a href="/update-task/<%= task._id %>" class="icon-btn edit">✏️</a>
                                   <a href="/delete/<%= task._id %>" class="icon-btn delete">🗑️</a>
                              </td>
                         </tr>
                         <% }) %>
               </tbody>
          </table>
     </div>
</body>

</html>